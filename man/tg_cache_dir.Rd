% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{tg_cache_dir}
\alias{tg_cache_dir}
\alias{tg_cache_pasta}
\title{Where cached responses are stored}
\usage{
tg_cache_dir(path = NULL)

tg_cache_pasta(path = NULL)
}
\arguments{
\item{path}{A directory to cache responses in, or \code{NULL} to report the
current one. The directory is created if it does not exist.}
}
\value{
The cache directory, invisibly when setting it.
}
\description{
Called with no argument, reports the directory in use. Called with a path,
switches to it for the rest of the session.
}
\details{
By default responses are cached in the session's temporary directory, so they
are discarded when R exits. To keep them between sessions, set this to a
persistent path, for example \code{tg_cache_dir(tools::R_user_dir("transferegovr", "cache"))}, or set the \code{TRANSFEREGOVR_CACHE_DIR} environment variable in your
\code{.Renviron}.

Caching is controlled by the \code{transferegovr.cache} option (\code{TRUE} by default)
and entries expire after \code{transferegovr.cache_ttl} seconds (3600 by default).
The data behind these APIs is refreshed daily.
}
\examples{
tg_cache_dir()
}
\seealso{
Other cache:
\code{\link[=tg_cache_clear]{tg_cache_clear()}}
}
\concept{cache}
