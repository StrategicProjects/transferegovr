% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{tg_params}
\alias{tg_params}
\alias{tg_parametros}
\title{List the parameters a table accepts as filters}
\usage{
tg_params(module, table)

tg_parametros(modulo, tabela)
}
\arguments{
\item{module}{A module name from \code{\link[=tg_modules]{tg_modules()}}. Aliases such as
\code{"fundo_a_fundo"} are accepted. \code{NULL} lists the tables of every module.}

\item{table}{A table name from \code{\link[=tg_tables]{tg_tables()}}.}

\item{modulo}{Portuguese alias for \code{module}, available in \code{\link[=tg_tabelas]{tg_tabelas()}} and
\code{\link[=tg_campos]{tg_campos()}}.}

\item{tabela}{Portuguese alias for \code{table}, available only in
\code{\link[=tg_campos]{tg_campos()}}.}
}
\value{
A tibble with one row per parameter: its name, the R type a value
should have, the type the API declares, the permitted values when the
parameter is enumerated, the pattern a value must match when it has one,
and its description.
}
\description{
Every parameter may be passed to \code{\link[=tg_get]{tg_get()}} and \code{\link[=tg_count]{tg_count()}} as a named
argument. Parameter names and their permitted values are in Portuguese
because they belong to the API.
}
\examples{
tg_params("parcerias", "proposta")

# Which parameters accept only a fixed set of values?
params <- tg_params("parcerias", "proposta")
params[lengths(params$values) > 0, c("param", "values")]
}
\seealso{
Other discovery:
\code{\link[=tg_fields]{tg_fields()}},
\code{\link[=tg_modules]{tg_modules()}},
\code{\link[=tg_schema_date]{tg_schema_date()}},
\code{\link[=tg_tables]{tg_tables()}},
\code{\link[=tg_updated_at]{tg_updated_at()}}
}
\concept{discovery}
